<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>{% block title %}{{title.label}}{% endblock %}</title>
        {% block stylesheets %}
            <link rel="stylesheet" type="text/css" href=" {{ asset('css/home.back.css') }}">
        {% endblock %}
    </head>
    <body>
        <nav>
            <ul>
                <li><a href="{{ path('bo_titleator') }}">Titleator</a></li>
            </ul>    
        </nav>
        <div>
            {% for info in app.flashes %}
            <p>{{ info }}</p>
            {% endfor %}
        </div>
        {% block body %}
            Hello world! Welcome to {{title.label}}
        {% endblock %}
        {% block javascripts %}{% endblock %}
    </body>
</html>